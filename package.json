{
  "name": "halftone",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --files '*.html, *.css, *.js' --no-notify"
  },
  "devDependencies": {
    "browser-sync": "^3"
  }
}
